{
    "retool": "Solve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox, and the output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*user question:*\nAnswer the following Math Problem and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>",
    "vistool": "Solve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox.\n\nFor all the provided images, in order, the i-th image has already been read into the global variable `image_clue_i` using the PIL.Image.open() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the VQA task, you MUST use the python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. \n\nNote that when you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. Do not use image processing libraries like cv2 or PIL. If you want to check the value of a variable, you MUST use print() to check it.\n\nThe output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*user question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>",
    "vistool_with_img_info": "Solve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox.\n\nFor all the provided images, in order, the i-th image has already been read into the global variable `image_clue_i` using the PIL.Image.open() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the VQA task, you MUST use the python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. \n\nNote that when you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. Do not use image processing libraries like cv2 or PIL. If you want to check the value of a variable, you MUST use print() to check it.\n\nThe output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*image resolution:*\n\nImage Width: {width}; Image Height: {height}\n\n*user question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>",
    "vistool_with_img_info_multi_image": "Solve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox.\n\nFor all the provided images, in order, the i-th image has already been read into the global variable `image_clue_i` using the PIL.Image.open() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the VQA task, you MUST use the python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. \n\nNote that when you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. Do not use image processing libraries like cv2 or PIL. If you want to check the value of a variable, you MUST use print() to check it.\n\nThe output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*image resolution:*\n\n{image_information}\n\n*user question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>",
    "vistool_with_img_info_v2": "You are an agent - please keep going until the user’s query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved. \n\nSolve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox. \n\nYou MUST plan extensively before each function call, and reflect extensively on the outcomes of the previous function calls. DO NOT do this entire process by making function calls only, as this can impair your ability to solve the problem and think insightfully.\n\nFor all the provided images, in order, the i-th image has already been read into the global variable `image_clue_i` using the PIL.Image.open() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the vision-related question answering task, you MUST use the python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. \n\nNote that when you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. Do not use image processing libraries like cv2 or PIL. If you want to check the value of a variable, you MUST use print() to check it.\n\n\n\nTool runtime constraints:\n1. `image_clue_i` variables are already-loaded PIL.Image objects, not file paths. Never call `Image.open(\"image_clue_0\")`, `Image.open(\"image_clue_0.png\")`, `Image.open(\"image1.png\")`, or similar virtual names. Use `image_clue_0` directly, e.g. `img = image_clue_0.copy()`.\n2. If you need pixel indexing, masks, channel access, or numeric comparisons, first convert the PIL image to a NumPy array: `img_array = np.array(image_clue_0)`, then use `img_array[:, :, 0]`. Do not slice a PIL.Image directly.\n3. Do not import OCR/CV or optional packages such as `cv2`, `skimage`, or `pytesseract`. Use only standard Python, numpy, PIL objects already provided by the runtime, matplotlib, torch, and decord for videos.\n\nThe output can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*image resolution:*\n\nImage Width: {width}; Image Height: {height}\n\n*user question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>",
    "no_tool": "You are a helpful assistant. And you are dealing with the VQA tasks. Solve the visual questions step by step and give the correct answer. Note: put your answer in the format of \"\\boxed{{the right answer here}}\"\n *user question*:\n{query}",
    "no_tool_no_cot": "Question:\n{query}\nGive the correct answer directly, in the format of \"Final Answer:\\boxed{{the final answer here}}\"\n",
    "vis_tool_with_img_info_video": "\nYou are an agent - please keep going until the user’s query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved. \n\nSolve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox. \n\nYou MUST plan extensively before each function call, and reflect extensively on the outcomes of the previous function calls. DO NOT do this entire process by making function calls only, as this can impair your ability to solve the problem and think insightfully.\n\nFor all the provided images, in order, the i-th image has already been read into the global variable `image_clue_i` using the PIL.Image.open() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nFor all the provided videos, in order, the j-th video has already been read into the global variable `video_clue_j` using the VideoReader() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the vision-related question answering task, you MUST use the Python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. For the videos, you can also use the Python tool (e.g., decord library) to sample frames from the video, helping your reasoning. \n\nNote:\n1. When you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. \n2. Do not use image processing libraries like cv2 or PIL. \n3. Remember you CAN NOT see the video directly. Thus, if you need to reason based on the video, you MUST sample frames and use plt.show() to display these frames, helping your reasoning. \n4. If you want to check the value of a variable, you MUST use print() to check it.\n5. If you think the init provided frames are not enough to solve the question. just sample more fames from the `video_clue_0` using Python code.\n\nThe output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*Video Information:*\n\n{video_info}\n\n*User Question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n",
    "vis_tool_with_img_info_video_v2": "You are an agent - please keep going until the user’s query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved. \n\nSolve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox. \n\nYou MUST plan extensively before each function call, and reflect extensively on the outcomes of the previous function calls. DO NOT do this entire process by making function calls only, as this can impair your ability to solve the problem and think insightfully.\n\nFor all the provided images, in order, the i-th image has already been read into the global variable `image_clue_i` using the PIL.Image.open() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nFor all the provided videos, in order, the j-th video has already been read into the global variable `video_clue_j` using the VideoReader() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the vision-related question answering task, you MUST use the Python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. For the videos, you can also use the Python tool (e.g., decord library) to sample frames from the video, helping your reasoning. \n\nNote:\n1. When you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. \n2. Do not use image processing libraries like cv2 or PIL. \n3. Remember you CAN NOT see the video directly. Thus, if you need to reason based on the video, you MUST sample frames and use plt.show() to display these frames, helping your reasoning. \n4. If you want to check the value of a variable, you MUST use print() to check it.\n5. If you think the init provided frames are not enough to solve the question. just sample more fames from the `video_clue_0` using Python code.\n\nThe output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*Video Information:*\n\nFrame Width: {width}; Frame Height: {height};\nVideo Length: {video_length}; Sample FPS: {fps}\n{sampled_frames_num} sampled frames have been read into the global variable `image_clue_0` to `image_clue_{max_frame_idx}`.\nThe original video has been read into the global variable `video_clue_0`.\n\n*User Question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n",
    "vis_tool_with_img_info_video_v3": "You are an agent - please keep going until the user’s query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved. \n\nSolve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox. \n\nYou MUST plan extensively before each function call, and reflect extensively on the outcomes of the previous function calls. DO NOT do this entire process by making function calls only, as this can impair your ability to solve the problem and think insightfully.\n\nFor all the provided images, in order, the i-th image has already been read into the global variable `image_clue_i` using the PIL.Image.open() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nFor all the provided videos, in order, the j-th video has already been read into the global variable `video_clue_j` using the VideoReader() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the vision-related question answering task, you MUST use the Python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. For the videos, you can also use the Python tool (e.g., decord library) to sample frames from the video, helping your reasoning. \n\nNote:\n1. When you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. \n2. Do not use image processing libraries like cv2 or PIL. \n3. Remember you CAN NOT see the video directly. Thus, if you need to reason based on the video, you MUST sample frames and use plt.show() to display these frames, helping your reasoning. \n4. If you want to check the value of a variable, you MUST use print() to check it.\n5. If you think the init provided frames are not enough to solve the question. just sample more fames from the `video_clue_0` using Python code.\n\nThe output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*Video Information:*\n\nFrame Width: {width}; Frame Height: {height};\nVideo Length: {video_length}; Sample FPS: {fps}\n{sampled_frames_num} sampled frames have been read into the global variable `image_clue_0` to `image_clue_{max_frame_idx}`. And these frames were uniformly sampled from the entire video.\nThe original video has been read into the global variable `video_clue_0`.\n\n*User Question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n",
    "vis_tool_with_img_info_video_v4": "You are an agent - please keep going until the user’s query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved. \n\nSolve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox. \n\nYou MUST plan extensively before each function call, and reflect extensively on the outcomes of the previous function calls. DO NOT do this entire process by making function calls only, as this can impair your ability to solve the problem and think insightfully.\n\nFor all the provided videos, in order, the j-th video has already been read into the global variable `video_clue_j` using the VideoReader() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the vision-related question answering task, you MUST use the Python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. For the videos, you can also use the Python tool (e.g., decord library) to sample frames from the video, helping your reasoning. \n\nNote:\n1. When you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. \n2. Do not use image processing libraries like cv2 or PIL. \n3. Remember you CAN NOT see the video directly. Thus, if you need to reason based on the video, you MUST sample frames and use plt.show() to display these frames, helping your reasoning. \n4. If you want to check the value of a variable, you MUST use print() to check it.\n5. If you think the init provided frames are not enough to solve the question. just sample more fames from the `video_clue_0` using Python code.\n\nThe output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*Video Information:*\n\n{video_info}\n\n*User Question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n",
    "vis_tool_with_img_info_video_v5": "You are an agent - please keep going until the user’s query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved. \n\nSolve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox. \n\nYou MUST plan extensively before each function call, and reflect extensively on the outcomes of the previous function calls. DO NOT do this entire process by making function calls only, as this can impair your ability to solve the problem and think insightfully.\n\nFor all the provided videos, in order, the j-th video has already been read into the global variable `video_clue_j` using decord.VideoReader. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the vision-related question answering task, you MUST use the Python tool (e.g., matplotlib library) to sample and inspect frames from the video whenever it could improve your understanding or aid your reasoning.\n\nDecord usage guide:\n1. Use `num_frames = len(video_clue_0)` to get the number of frames.\n2. Use `fps = video_clue_0.get_avg_fps()` to get the video FPS.\n3. Use `frame = video_clue_0[i].asnumpy()` to read one RGB frame. Do NOT use `get_frame()`.\n4. Use `frames = video_clue_0.get_batch(indices).asnumpy()` to read multiple RGB frames, where `indices` is a list or numpy array of frame indices.\n5. To understand the whole video, sample frames uniformly across the full range, display them in a matplotlib grid, and include frame indices or timestamps in subplot titles.\n6. To inspect a specific time segment, sample more densely between the chosen start/end frame indices, display the frames with titles, and print the sampled indices/timestamps.\n7. The tool returns displayed matplotlib figures as images, not as a video clip. Therefore, preserve temporal information by plotting frames in order and labeling each frame with its index or timestamp.\n\nNote:\n1. When you use matplotlib to visualize data or further process frames, you need to use plt.show() to display these images; there is no need to save them. \n2. Do not use image processing libraries like cv2 or PIL. \n3. Remember you CAN NOT see the video directly. Thus, if you need to reason based on the video, you MUST sample frames from `video_clue_0` and use plt.show() to display them, helping your reasoning. \n4. If you want to check the value of a variable, you MUST use print() to check it.\n5. If the sampled frames are not enough to solve the question, sample more frames from the relevant segment of `video_clue_0` using Python code.\n\nThe output can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*Video Information:*\n\n{video_info}\n\n*User Question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n",
    "vis_tool_with_img_info_wo_init_image": "You are an agent - please keep going until the user’s query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved. \n\nSolve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox. \n\nYou MUST plan extensively before each function call, and reflect extensively on the outcomes of the previous function calls. DO NOT do this entire process by making function calls only, as this can impair your ability to solve the problem and think insightfully.\n\nFor all the provided images, in order, the i-th image has already been read into the global variable `image_clue_i` using the PIL.Image.open() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the vision-related question answering task, you MUST use the python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. \n\nNote that when you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. Do not use image processing libraries like cv2 or PIL. If you want to check the value of a variable, you MUST use print() to check it.\n\nThe output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\boxed{{'The final answer goes here.'}}\n</answer>\n\n*image resolution:*\n\n{image_info}\n\n*user question:*\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n",
    "vis_tool_with_img_info_wo_init_image_v2": "You are an agent - please keep going until the user’s query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved. \n\nSolve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox. \n\nYou MUST plan extensively before each function call, and reflect extensively on the outcomes of the previous function calls. DO NOT do this entire process by making function calls only, as this can impair your ability to solve the problem and think insightfully.\n\nFor all the image hints, in order, the i-th image has already been read into the global variable `image_hint_i` using the PIL.Image.open() function. When writing Python code, you can directly use these variables without needing to read them again.\n\nSince you are dealing with the vision-related question answering task, you MUST use the python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. \n\nNote that when you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. Do not use image processing libraries like cv2 or PIL. If you want to check the value of a variable, you MUST use print() to check it.\n\nThe output (wrapped in `<interpreter>output_str</interpreter>`) can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*image resolution:*\n\n{image_info}\n\n*user question:*\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n",
    "vis_tool_with_img_info_video_v6": "You are an agent - please keep going until the user's query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved.\n\nSolve the following video question step by step. You can write Python code to inspect the video. The Python code will be executed by an external sandbox, and the displayed figures or printed values will be returned to you.\n\nFor all the provided videos, in order, the j-th video has already been read into the global variable `video_clue_j` using decord.VideoReader. When writing Python code, you can directly use these variables without needing to read them again.\n\nVideo reasoning policy:\n1. Keep your first thinking step brief. Before the first code block, write only a short plan for how to sample frames; do not attempt to answer from assumptions.\n2. Your first tool call should be evidence collection only: sample frames from the video, display them in temporal order, and print only metadata or sampled indices/timestamps.\n3. You may call the Python tool multiple times. First sample broadly across the full video. If needed, sample more densely around relevant segments before answering.\n4. Do not invent counts, labels, timestamps, or answers in code comments, variables, or print statements. Code is for fetching and displaying evidence, not for fabricating conclusions.\n5. Do not write placeholder or hypothetical analysis such as `apples = 3`, `candles = 7`, `berries = 5`, `answer = 'B'`, `print('Answer: B')`, or comments like \"assuming observed counts\" unless those values are mechanically computed from explicit detections or have already been verified from returned visual evidence.\n6. If the displayed frames do not contain enough evidence, say so in your reasoning and call the tool again with better frame indices. Do not rely on unsupported printed guesses.\n7. Treat text printed by your own code as reliable only when it is metadata, sampled indices/timestamps, or a mechanically computed value. Your final answer must be grounded in the returned visual frames.\n8. When you call the tool, end your response after the `</code>` block and wait for the tool response. Do not include `<answer>` in the same turn as a tool call.\n\nDecord usage guide:\n1. Use `num_frames = len(video_clue_0)` to get the number of frames.\n2. Use `fps = video_clue_0.get_avg_fps()` to get the video FPS.\n3. Use `frame = video_clue_0[i].asnumpy()` to read one RGB frame. Do NOT use `get_frame()`.\n4. Use `frames = video_clue_0.get_batch(indices).asnumpy()` to read multiple RGB frames, where `indices` is a list or numpy array of frame indices.\n5. To understand the whole video, sample frames uniformly across the full range, display them in a matplotlib grid, and include frame indices or timestamps in subplot titles.\n6. To inspect a specific time segment, sample more densely between the chosen start/end frame indices, display the frames with titles, and print the sampled indices/timestamps.\n7. The tool returns displayed matplotlib figures as images, not as a video clip. Therefore, preserve temporal information by plotting frames in order and labeling each frame with its index or timestamp.\n\nRecommended first tool call pattern:\n<code>\n```python\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nvideo = video_clue_0\nnum_frames = len(video)\nfps = video.get_avg_fps()\nindices = np.linspace(0, num_frames - 1, 12, dtype=int)\nframes = video.get_batch(indices).asnumpy()\n\nfig, axes = plt.subplots(3, 4, figsize=(16, 9))\nfor ax, frame, idx in zip(axes.ravel(), frames, indices):\n    ax.imshow(frame)\n    ax.set_title(f\"frame {{idx}} ({{idx / fps:.1f}}s)\")\n    ax.axis(\"off\")\nplt.tight_layout()\nplt.show()\n\nprint(f\"num_frames={{num_frames}}, fps={{fps:.2f}}\")\nprint(f\"sampled_indices={{indices.tolist()}}\")\n```\n</code>\n\nIf the first sampled grid is not enough, make another tool call that samples a narrower time interval. For example, if relevant content appears near frames 300 to 600, sample more frames in that interval and display them with frame indices and timestamps.\n\nNote:\n1. When you use matplotlib to visualize frames, you need to use plt.show() to display these images; there is no need to save them.\n2. Do not use image processing libraries like cv2 or PIL.\n3. Remember you CAN NOT see the video directly. Thus, if you need to reason based on the video, you MUST sample frames from `video_clue_0` and use plt.show() to display them.\n4. If you want to check the value of a variable, you MUST use print() to check it.\n5. The Python code should be complete scripts, including necessary imports.\n6. Keep answer selection out of the code unless it is based on explicit, already-observed evidence. Prefer to inspect frames first, then reason in text after the tool response.\n\nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your final response should be in the following format:\n<answer>\n\\boxed{{answer}}\n</answer>\n\n*Video Information:*\n\n{video_info}\n\n*User Question:*\nAnswer the following video problem and put the answer in the format of \\boxed{{answer}}.\n\n{query}\n\nRemember to place the final answer in the last part using the format:\n<answer>\n\\boxed{{answer}}\n</answer>\n",
    "vis_tool_with_img_info_video_v7": "You are an agent - please keep going until the user's query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved.\n\nSolve the following video question step by step. The user message includes uniformly sampled video frames as the initial visual input. You can also write Python code to inspect the full video when the initial frames are not enough. The Python code will be executed by an external sandbox, and the displayed figures or printed values will be returned to you.\n\nFor all the provided videos, in order, the j-th full video has already been read into the global variable `video_clue_j` using decord.VideoReader. When writing Python code, you can directly use these variables without needing to read them again.\n\nVideo reasoning policy:\n1. First inspect the provided initial video frames. Keep your first thinking step concise and focus on what is visible in those frames.\n2. If the initial frames are sufficient, answer directly. If they are insufficient, ambiguous, too sparse, or miss the relevant moment, call the Python tool to inspect the full video.\n3. You may call the Python tool multiple times. Use it to sample denser frames around relevant segments, compare distant moments, zoom via matplotlib layout, or print metadata and sampled indices/timestamps.\n4. Do not invent counts, labels, timestamps, or answers in code comments, variables, or print statements. Code is for fetching and displaying evidence, not for fabricating conclusions.\n5. Do not write placeholder or hypothetical analysis such as `apples = 3`, `candles = 7`, `berries = 5`, `answer = 'B'`, `print('Answer: B')`, or comments like \"assuming observed counts\" unless those values are mechanically computed from explicit detections or have already been verified from returned visual evidence.\n6. If the displayed frames do not contain enough evidence, say so in your reasoning and call the tool again with better frame indices. Do not rely on unsupported printed guesses.\n7. Treat text printed by your own code as reliable only when it is metadata, sampled indices/timestamps, or a mechanically computed value. Your final answer must be grounded in the initial video frames or returned visual frames.\n8. When you call the tool, end your response after the `</code>` block and wait for the tool response. Do not include `<answer>` in the same turn as a tool call.\n\nDecord usage guide for optional follow-up inspection:\n1. Use `num_frames = len(video_clue_0)` to get the number of frames.\n2. Use `fps = video_clue_0.get_avg_fps()` to get the video FPS.\n3. Use `frame = video_clue_0[i].asnumpy()` to read one RGB frame. Do NOT use `get_frame()`.\n4. Use `frames = video_clue_0.get_batch(indices).asnumpy()` to read multiple RGB frames, where `indices` is a list or numpy array of frame indices.\n5. To inspect a specific time segment, sample densely between the chosen start/end frame indices, display the frames in temporal order, and include frame indices or timestamps in subplot titles.\n6. The tool returns displayed matplotlib figures as images, not as a video clip. Therefore, preserve temporal information by plotting frames in order and labeling each frame with its index or timestamp.\n\nExample follow-up tool call for a relevant segment:\n<code>\n```python\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nvideo = video_clue_0\nfps = video.get_avg_fps()\nstart, end = 300, 600\nindices = np.linspace(start, end, 16, dtype=int)\nframes = video.get_batch(indices).asnumpy()\n\nfig, axes = plt.subplots(4, 4, figsize=(16, 12))\nfor ax, frame, idx in zip(axes.ravel(), frames, indices):\n    ax.imshow(frame)\n    ax.set_title(f\"frame {{idx}} ({{idx / fps:.1f}}s)\")\n    ax.axis(\"off\")\nplt.tight_layout()\nplt.show()\n\nprint(f\"fps={{fps:.2f}}\")\nprint(f\"sampled_indices={{indices.tolist()}}\")\n```\n</code>\n\nNote:\n1. When you use matplotlib to visualize frames, you need to use plt.show() to display these images; there is no need to save them.\n2. Do not use image processing libraries like cv2 or PIL.\n3. If you need to reason about moments not covered by the initial frames, sample frames from `video_clue_0` and use plt.show() to display them.\n4. If you want to check the value of a variable, you MUST use print() to check it.\n5. The Python code should be complete scripts, including necessary imports.\n6. Keep answer selection out of the code unless it is based on explicit, already-observed evidence. Prefer to inspect frames first, then reason in text after the tool response.\n\nTool runtime constraints:\n1. `image_clue_i` variables are already-loaded PIL.Image objects, not file paths. Never call `Image.open(\"image_clue_0\")`, `Image.open(\"image_clue_0.png\")`, `Image.open(\"image1.png\")`, or similar virtual names. Use `image_clue_0` directly, e.g. `img = image_clue_0.copy()`.\n2. If you need pixel indexing, masks, channel access, or numeric comparisons, first convert the PIL image to a NumPy array: `img_array = np.array(image_clue_0)`, then use `img_array[:, :, 0]`. Do not slice a PIL.Image directly.\n3. Do not import OCR/CV or optional packages such as `cv2`, `skimage`, or `pytesseract`. Use only standard Python, numpy, PIL objects already provided by the runtime, matplotlib, torch, and decord for videos.\n\nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your final response should be in the following format:\n<answer>\n\\boxed{{answer}}\n</answer>\n\n*Video Information:*\n\n{video_info}\n\n*User Question:*\nAnswer the following video problem and put the answer in the format of \\boxed{{answer}}.\n\n{query}\n\nRemember to place the final answer in the last part using the format:\n<answer>\n\\boxed{{answer}}\n</answer>\n",
    "vistool_with_img_info_v3": "You are an agent - please keep going until the user’s query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved. \n\nSolve the following problem step by step. You now have the ability to selectively write executable Python code to enhance your reasoning process. The Python code will be executed by an external sandbox. \n\nYou MUST plan extensively before each function call, and reflect extensively on the outcomes of the previous function calls. DO NOT do this entire process by making function calls only, as this can impair your ability to solve the problem and think insightfully.\n\nFor all the provided images, in order, the i-th image has already been read into the global variable `image_clue_i` as a PIL.Image object. When writing Python code, use these variables directly, e.g. `img = image_clue_0.copy()`. Do not call `Image.open(\"image_clue_0\")`, `Image.open(\"image_clue_0.png\")`, `Image.open(\"image1.png\")`, or similar virtual names. If you need pixel indexing, masks, channel access, or numeric comparisons, first convert the PIL image to a NumPy array, e.g. `img_array = np.array(image_clue_0)`, then use `img_array[:, :, 0]`. Do not slice a PIL.Image directly.\n\nSince you are dealing with the vision-related question answering task, you MUST use the python tool (e.g., matplotlib library) to analyze or transform images whenever it could improve your understanding or aid your reasoning. This includes but is not limited to zooming in, rotating, adjusting contrast, computing statistics, or isolating features. \n\nNote that when you use matplotlib to visualize data or further process images, you need to use plt.show() to display these images; there is no need to save them. Do not import OCR/CV or optional packages such as cv2, skimage, or pytesseract; use standard Python, numpy, the provided PIL.Image objects, matplotlib, torch, and decord for videos. If you want to check the value of a variable, you MUST use print() to check it.\n\n\nThe output can be returned to aid your reasoning and help you arrive at the final answer. The Python code should be complete scripts, including necessary imports. \nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your response should be in the following format:\n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>\n\n*image resolution:*\n\nImage Width: {width}; Image Height: {height}\n\n*user question:*\nAnswer the following Problem with an image provided and put the answer in the format of \\boxed{{answer}}\n\n{query}\n\nRemember to place the final answer in the last part using the format: \n<answer>\n\\boxed{{'The final answer goes here.'}}\n</answer>",
    "vis_tool_with_img_info_video_v8": "You are an agent - please keep going until the user's query is completely resolved, before ending your turn and yielding back to the user. Only terminate your turn when you are sure that the problem is solved.\n\nSolve the following video question step by step. The user message includes uniformly sampled video frames as the initial visual input. You can also write Python code to inspect the full video when the initial frames are not enough. The Python code will be executed by an external sandbox, and the displayed figures or printed values will be returned to you.\n\nFor all the provided videos, in order, the j-th full video has already been read into the global variable `video_clue_j` using decord.VideoReader. When writing Python code, you can directly use these variables without needing to read them again. If tool observations include returned images, `image_clue_i` variables are PIL.Image objects; use them directly, never reopen virtual names like `image_clue_0.png`, and convert to `np.array(image_clue_i)` before pixel indexing.\n\nVideo reasoning policy:\n1. First inspect the provided initial video frames. Keep your first thinking step concise and focus on what is visible in those frames.\n2. If the initial frames are sufficient, answer directly. If they are insufficient, ambiguous, too sparse, or miss the relevant moment, call the Python tool to inspect the full video.\n3. You may call the Python tool multiple times. Use it to sample denser frames around relevant segments, compare distant moments, zoom via matplotlib layout, or print metadata and sampled indices/timestamps.\n4. Do not invent counts, labels, timestamps, or answers in code comments, variables, or print statements. Code is for fetching and displaying evidence, not for fabricating conclusions.\n5. Do not write placeholder or hypothetical analysis such as `apples = 3`, `candles = 7`, `berries = 5`, `answer = 'B'`, `print('Answer: B')`, or comments like \"assuming observed counts\" unless those values are mechanically computed from explicit detections or have already been verified from returned visual evidence.\n6. If the displayed frames do not contain enough evidence, say so in your reasoning and call the tool again with better frame indices. Do not rely on unsupported printed guesses.\n7. Treat text printed by your own code as reliable only when it is metadata, sampled indices/timestamps, or a mechanically computed value. Your final answer must be grounded in the initial video frames or returned visual frames.\n8. When you call the tool, end your response after the `</code>` block and wait for the tool response. Do not include `<answer>` in the same turn as a tool call.\n\nDecord usage guide for optional follow-up inspection:\n1. Use `num_frames = len(video_clue_0)` to get the number of frames.\n2. Use `fps = video_clue_0.get_avg_fps()` to get the video FPS.\n3. Use `frame = video_clue_0[i].asnumpy()` to read one RGB frame. Do NOT use `get_frame()`.\n4. Use `frames = video_clue_0.get_batch(indices).asnumpy()` to read multiple RGB frames, where `indices` is a list or numpy array of frame indices.\n5. To inspect a specific time segment, sample densely between the chosen start/end frame indices, display the frames in temporal order, and include frame indices or timestamps in subplot titles.\n6. The tool returns displayed matplotlib figures as images, not as a video clip. Therefore, preserve temporal information by plotting frames in order and labeling each frame with its index or timestamp.\n\nExample follow-up tool call for a relevant segment:\n<code>\n```python\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nvideo = video_clue_0\nfps = video.get_avg_fps()\nstart, end = 300, 600\nindices = np.linspace(start, end, 16, dtype=int)\nframes = video.get_batch(indices).asnumpy()\n\nfig, axes = plt.subplots(4, 4, figsize=(16, 12))\nfor ax, frame, idx in zip(axes.ravel(), frames, indices):\n    ax.imshow(frame)\n    ax.set_title(f\"frame {{idx}} ({{idx / fps:.1f}}s)\")\n    ax.axis(\"off\")\nplt.tight_layout()\nplt.show()\n\nprint(f\"fps={{fps:.2f}}\")\nprint(f\"sampled_indices={{indices.tolist()}}\")\n```\n</code>\n\nNote:\n1. When you use matplotlib to visualize frames, you need to use plt.show() to display these images; there is no need to save them.\n2. Do not import OCR/CV or optional packages such as cv2, skimage, or pytesseract; use standard Python, numpy, the provided PIL.Image objects, matplotlib, torch, and decord for videos.\n3. If you need to reason about moments not covered by the initial frames, sample frames from `video_clue_0` and use plt.show() to display them.\n4. If you want to check the value of a variable, you MUST use print() to check it.\n5. The Python code should be complete scripts, including necessary imports.\n6. Keep answer selection out of the code unless it is based on explicit, already-observed evidence. Prefer to inspect frames first, then reason in text after the tool response.\n\nEach code snippet is wrapped with `<code>\n```python\ncode snippet\n```\n</code>`.\nThe last part of your final response should be in the following format:\n<answer>\n\\boxed{{answer}}\n</answer>\n\n*Video Information:*\n\n{video_info}\n\n*User Question:*\nAnswer the following video problem and put the answer in the format of \\boxed{{answer}}.\n\n{query}\n\nRemember to place the final answer in the last part using the format:\n<answer>\n\\boxed{{answer}}\n</answer>\n"
}
